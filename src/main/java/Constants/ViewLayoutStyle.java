package Constants;

/**
 * Formats two components in either a horizontal or vertical layout in relation to each other.
 * Set {@code style} to {@code ViewLayoutStyle.VERTICAL} to achieve a layout like this: A|B.
 */
public enum ViewLayoutStyle {
    HORIZONTAL,
    VERTICAL
}
